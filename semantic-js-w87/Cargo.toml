[package]
name = "semantic-js-w87"
version = "0.1.0"
edition = "2021"

[dependencies]
bitflags = "2.6.0"
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
