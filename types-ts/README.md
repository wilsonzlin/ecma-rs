# Deprecated

This crate has been superseded by `types-ts-interned` and has been removed from
the workspace. It remains for historical reference only; new development should
target `types-ts-interned`.
